eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:eureka@localhost:8000/eureka/

spring:
  cloud:
    config:
      name: warriors-server-multisource
      discovery:
        enabled: true
        serviceId: warriors-config-center
      label: master
      profile: dev
    bus:
      enabled: true

management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
